query characterDetails ($id: Int) {
    Character(id: $id) {
        id
        name{
            full
        }
        image {
            medium
        }
        description(asHtml:true)
        gender
        age
    }
}